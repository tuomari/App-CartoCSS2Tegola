#!/usr/bin/env perl

# PODNAME: cartocss2tegola

# ABSTRACT: Convert CartoCSS project file to tegola configuration

use 5.010001;
use strict;
use warnings;

use FindBin;
use lib "$FindBin::Bin/../lib";

use App::CartoCSS2Tegola;

App::CartoCSS2Tegola->new_with_options()->run() unless caller();
